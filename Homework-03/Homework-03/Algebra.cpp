//
//  Algebra.cpp
//  Homework-03
//
//  Created by Daniel Portillo on 9/28/16.
//  Copyright © 2016 Daniel Portillo. All rights reserved.
//

#include "Algebra.hpp"

/*
int main()
{
    double a, b, c, discriminant;
    cout << "Enter a: ";
    cin >> a;
    cout << "Enter b: ";
    cin >> b;
    cout << "Enter c: ";
    cin >> c;
    
    QuadraticEquation equation(a, b, c);
    discriminant = equation.getDiscriminant();
    
    if (discriminant < 0)
    {
        cout << "The equation has no roots\n";
    }
    else if (discriminant == 0)
    {
        cout << "\nThe root is " << equation.getRoot1() << endl;
    }
    else // (discriminant >= 0)
    {
        cout << "\nThe roots are " << equation.getRoot1() << " and "
        << equation.getRoot2() << endl;
    }
    
    return 0;
}*/